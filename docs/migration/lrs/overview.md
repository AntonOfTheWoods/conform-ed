# LRS Migration Overview

Tracks the LRS tester and its container packaging as the implementation evolves from the current copied upstream runner into the production OCI/compose workflow.

## LRS Migration Documents

- `source-policy.md`: external source and copy boundaries.
- `parity-strategy.md`: parity and oracle behavior strategy.
- `traceability.md`: migration tracking and evidence.
- `typing-migration-protocol.md`: type-first migration runbook across `lrs-conformance-ts` and `conform-ed`.
