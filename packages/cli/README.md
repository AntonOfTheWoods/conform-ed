# @conform-ed/cli

Shared CLI primitives.

Current QTI command helpers:

- inventory official/example corpora
- validate a single QTI XML file
- validate a folder of QTI XML files against the currently supported normalization slice
- validate exploded QTI package directories rooted at `imsmanifest.xml`
- generate a coverage report that separates root-level coverage from actual validation success
